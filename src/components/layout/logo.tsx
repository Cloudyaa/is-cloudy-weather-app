import type { ReactNode } from 'react';

export function Logo(): ReactNode {
  return (
    <span className="font-black text-primary px-2 py-1 rounded tracking-tighter text-lg">
      isCloudy
    </span>
  );
}
